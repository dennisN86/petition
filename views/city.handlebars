<h1>Those are the people from {{city}} who participated in the petition</h1>

{{#listOfParticipants}}

    <p>

    {{#if url}}

        <a href="{{ url }}">{{ first_name }} {{ last_name }}</a>

    {{ else }}

        {{ first_name }} {{ last_name }}

    {{/if }}

    {{#if age}}

        || {{ age }}

    {{/if }}

    </p>

{{/listOfParticipants}}

<!-- find fancy placeholder for || -->
