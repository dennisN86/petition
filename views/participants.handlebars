<h1>Those are the people who participated in the petition</h1>

{{#listOfParticipants}}

    <p><a href="{{ url }}">{{ first_name }} {{ last_name }}</a>, {{ age }}, <a href="/participants/{{city}}">{{ city }}</a></p>

{{/listOfParticipants}}
